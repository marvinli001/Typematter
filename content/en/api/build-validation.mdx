---
title: Build validation
order: 2
section: API
type: api
status: experimental
version: 0.4.0-rc
description: API reference for document validation commands, CI integration, and blocking rules.
aliases:
  - validate docs
  - validation
---

## CLI

- `typematter validate`
- `typematter build` runs validation first

Validation runs in CI/CD to protect doc quality.

## Checklist

- Broken links (internal routes and anchors)
- Duplicate titles
- Orphan pages
- Invalid frontmatter fields
- Empty directories

## CI integration

Run `validate:docs` before the build. Any error blocks the build.

<Callout type="warning" title="Strict rule">
Any duplicate route will fail the build.
</Callout>


