---
title: 快速开始
order: 2
section: 开始使用
type: guide
pager: true
description: Starter 快速开始，涵盖安装依赖和替换示例内容的最短路径。
---

## 1. 安装依赖

```bash
npm install
npm run dev
```

## 2. 替换示例内容

删除 starter 示例页面内容，按你的目录结构新建 MDX 页面。

```text
content/
  cn/
    index.mdx
    get-started/
      quickstart.mdx
  en/
    index.mdx
```

## 3. 调整导航

导航配置在 `nav.config.ts`，它只负责展示顺序，不负责创造页面。

<Callout type="note" title="前提">
每个页面至少需要 `title`、`order`、`section` 三个 frontmatter 字段。
</Callout>
